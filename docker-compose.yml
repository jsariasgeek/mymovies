services:
  mymovies:
    build:
      context: .
    ports:
      - '5173:5173'
    volumes:
      - .:/app
      - /app/node_modules
      - ~/.aws:/root/.aws:ro
    environment:
      - CHOKIDAR_USEPOLLING=true # Needed for hot reload in some systems
    command: npm run dev -- --host
